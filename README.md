# Project Structure

- `relay-server/`: Railway WebSocket relay server
- `windows-client/`: Windows input receiver client
- `android-client/`: Android overlay keypad client

The server implementation currently lives under `relay-server/`.
